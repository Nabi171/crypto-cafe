import React from 'react';

const SingeleCoinDetails = (id) => {
    return (
        <div>
            <h1>this is coin details page ${id}</h1>
        </div>
    );
};

export default SingeleCoinDetails;